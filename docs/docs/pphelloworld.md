## pphelloworld

Simple testing program. All outputs start toggling with frequency streamer_clk/1000, i.e. 100kHz
with the default clock settings.

Command line parameters: `-core_pll` and `-int_pll` settings.
