Command recipe files for PulsePins tools.

These files were moved out of `tests/` so test executables and reusable command examples are stored separately.
Each file is named after the corresponding tool, for example `recipes/pptest` or `recipes/ppfreq`.
