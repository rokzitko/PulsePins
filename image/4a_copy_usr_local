#!/bin/bash
set -euo pipefail

ORIG=$HOME/yocto/local/
MOUNTPOINT="$(pwd)/ext"

if ! mountpoint -q "$MOUNTPOINT"; then
	echo "error: $MOUNTPOINT is not mounted" >&2
	exit 1
fi

sudo cp -rv "$ORIG" "$MOUNTPOINT/usr/"
